#include<stdio.h>
main()
{
	struct birthday{
		char name[100];
		int month;
		int date;
	};
	int i,n;
	scanf("%d",&n);
	struct birthday bir[n];
	for(i=0;i<n;i++)
	{
		scanf("%s",bir[i].name);
		scanf("%d %d",&bir[i].month,&bir[i].date);

	 } 
	 for(i=0;i<n;i++){
	 	
	 }
 } 
